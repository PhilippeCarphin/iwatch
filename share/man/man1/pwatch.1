.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "iwatch" "" "" "" ""
.hy
.SH NAME
.IP
.nf
\f[C]
pwatch
\f[R]
.fi
.SH SYNOPSIS
.IP
.nf
\f[C]
iwatch --filespec FILESPEC --cmd CMD [--recursive]
\f[R]
.fi
.SH DESCRIPTION
.PP
Run \f[C]CMD\f[R] every time one of \f[C]FILESPEC\f[R] is written to.
.PP
\f[C]FILESPEC\f[R]: A comma, space, or newline separated list of files.
.PP
\f[C]CMD\f[R]: Command to run when an event happens.
.PP
If \f[C]CMD\f[R] is long running the previous run of \f[C]CMD\f[R] is
sent the \f[C]TERM\f[R] signal and a new run is started when an event
happens.
.SH CAVEATS
.PP
\f[C]CMD\f[R] is run whenever certain events happen to the watched files
so not exactly when the file is written to.
Other events are ignored like opening the file which happens very
frequently if we have an LSP running.
.SH BUGS AND CONTRIBUTIONS
.PP
Please submit an issue on <https://github.com/philippecarphin/iwatch> or
submit a merge request with any improvements.
.SH LORE
.PP
The p is for Python.
I only wrote that version in 2025 because I wanted to see if it was
going to be easy.
It turned out to be a bit more difficult because I had to use threads
and flags to make the threads stop.
Still fun though.
.PP
It turns out that this is still useful because it\[aq]s easier to pip
install watchdog than to get fswatch or inotify-tools.
.SH AUTHOR
.PP
Philippe Carphin
